git
head - 현재 위치
git branch - 브랜치 생성
git switch - 브랜치 이동
메인(마스터)로 이동 후에 merge - 합치기

merge 방식
[3-way-merge]
2 - ㄱ
1 -  - 1
일반적인 merge

[fast-foward-merge]
기존 main에 신규 변경점이 없는 경우

branch -D (-d) - 삭제

rebase - 신규 변경점이 있어도 fast-foward-merge 가능. 로그가 깔끔해보임
새로운 브랜치로 이동 후에 git rebase 후에 main에서 merge

squash - 여러개의 커밋을 하나로 합침. merge 시에 세부 커밋이 깔끔하게 보임

git restore - 최근 버전으로 파일 복구
git resotre --source 커밋ID 파일명 - 특정 커밋으로 파일 복구
git resotre --staged 스테이징 취소
git revert 커밋ID - 특정 커밋 취소. (여러 커밋ID 가능)
git reset --hard 커밋ID - 특정 커밋으로 이동. 이후 커밋 삭제
